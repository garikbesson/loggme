class LogMe_App {
    constructor (store) {
        this.store = store;
        // this.store.dispatch(checkUserToken());
        // document.body.addEventListener('click', ()=> {this.store.dispatch(checkUserToken());});//TODO remove this
        // this.store.dispatch(loadScorecard());
        // this.store.dispatch(loadComments());
    }
}

export default LogMe_App;